{
  "name": "sofya-portfolio-2026",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "deploy:check": "npm run build && wrangler deploy --dry-run"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "lucide": "^1.28.0"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "wrangler": "^4.118.0"
  }
}
